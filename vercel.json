{
  "buildCommand": "npm install && npm install -D tailwindcss postcss autoprefixer @types/node @types/react typescript && npm run lint -- --max-warnings 0 || true && tsc --noEmit || true && NODE_ENV=production next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "github": {
    "silent": false,
    "autoJobCancelation": false
  }
} 